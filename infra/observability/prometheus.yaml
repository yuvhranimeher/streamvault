global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: streamvault-edge
    metrics_path: /metrics
    static_configs:
      - targets: [edge:4000]

rule_files:
  - /etc/prometheus/streamvault-alerts.yaml
